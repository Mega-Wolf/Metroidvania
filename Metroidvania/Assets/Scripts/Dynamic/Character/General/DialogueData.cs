using UnityEngine;

public struct DialogueData {
    public string Name;

    public int DialogueID; //(not sure if I don't just get that with the Name)
    public Sprite Sprite; //(same as DialogueID)
}